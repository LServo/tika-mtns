
class User {
    constructor
    
    /**
     * id
     * nick
     * email
     * conta
     * isAdmin
     * coplayers Coplayer[]
     * created_at
     */
}

class Account {
    nickname: Tikabum
    user_admin_id: [new User(Lucas).ID]
    password: string,
    hash_password: string,
    coplayers: [new User(Gabriel), new User(Lucas)],
}

// Tikabum = Gabriel e o Lucas

// Tika - User => O que um user pode fazer diferente do coplayer?
// Mtns - User => 